import 'package:flutter/material.dart';

class ADHDHome extends StatelessWidget {
  const ADHDHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('ADHD Module'),
      ),
    );
  }
}
