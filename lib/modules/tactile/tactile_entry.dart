import 'package:flutter/material.dart';
import 'tactile_home.dart';

class TactileEntry extends StatelessWidget {
  const TactileEntry({super.key});

  @override
  Widget build(BuildContext context) {
    return const TactileHome();
  }
}
