import 'package:flutter/material.dart';

class TactileHome extends StatelessWidget {
  const TactileHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Tactile Module'),
      ),
    );
  }
}
